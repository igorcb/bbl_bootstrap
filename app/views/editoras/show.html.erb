<%= provide(:title, @editora.descricao) %>
<p>
	<strong>Descrição:</strong>
	<%= @editora.descricao %>
</p>

<p>
	<strong>Cidade:</strong>
	<%= @editora.cidade %>
</p>

<p>
	<strong>Ano:</strong>
	<%= @editora.ano %>
</p>

<div class="form-actions">
  <%= link_to t('views.new'), new_editora_path, class: "btn btn-primary" %>
  <%= link_to t('views.edit'), edit_editora_path(@editora), class: "btn" %>
  <%= link_to t('views.list'), editoras_path, class: "btn" %>
</div>

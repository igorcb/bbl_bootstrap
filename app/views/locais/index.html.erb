<% provide(:title, 'Locais') %>
<h1><%= t('views.listing') %> locais</h1>

<%= link_to "#{t('views.new')} local", new_local_path, class: "btn btn-primary btn-large btn-block"  %>

<%= will_paginate %>

<table class="table table-condensed table-hover">
	<thead>
		<th>Descrição</th>
		<th colspan="3">Opções</th>
	</thead>
	<tbody>

    <%= render @locais %>

	</tbody>
</table>

<%= will_paginate %>

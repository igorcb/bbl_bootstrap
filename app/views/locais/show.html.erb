<%= provide(:title, @local.descricao) %>
<p>
	<strong>Local:</strong>
	<%= @local.descricao %>
</p>

<div class="form-actions">
  <%= link_to "New", new_local_path, class: "btn btn-primary" %>
  <%= link_to "Edit", edit_local_path(@local), class: "btn" %>
  <%= link_to "All", locais_path, class: "btn" %>
</div>

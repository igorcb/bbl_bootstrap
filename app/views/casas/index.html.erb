<% provide(:title, 'Casas') %>
<h1><%= t('views.listing') %> casas</h1>

<%= link_to "#{t('views.new_female')} casa", new_casa_path, class: "btn btn-primary btn-large btn-block"  %>

<%= will_paginate %>

<table class="table table-condensed table-hover">
	<thead>
		<th>Descrição</th>
		<th colspan="3">Opções</th>
	</thead>
	<tbody>

    <%= render @casas %>

	</tbody>
</table>

<%= will_paginate %>
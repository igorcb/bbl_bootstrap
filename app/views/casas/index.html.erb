<% provide(:title, 'Casas') %>
<h1>Listagem de casas</h1>

<%= link_to "Nova Casa", new_casa_path, class: "btn btn-primary btn-large btn-block"  %>

<%= will_paginate %>

<table class="table table-condensed table-hover">
	<thead>
		<th>Descrição</th>
		<th colspan="3">Opções</th>
	</thead>
	<tbody>

    <%= render @casas %>

	</tbody>
</table>

<%= will_paginate %>
<% provide(:title, 'Classificações') %>
<h1><%= t('views.listing') %> classificações</h1>

<%= link_to "#{t('views.new_female')} classificação", new_classificacao_path, class: "btn btn-primary btn-large btn-block"  %>

<%= will_paginate %>

<table class="table table-condensed table-hover">
	<thead>
		<th>Descrição</th>
		<th>Cdd</th>
		<th colspan="3">Opções</th>
	</thead>
	<tbody>

    <%= render @classificacoes %>

	</tbody>
</table>

<%= will_paginate %>
